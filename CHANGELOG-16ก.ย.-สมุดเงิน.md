# สมุดเงิน — รอบ 1 ของแผนเทียบ FlowAccount
_16 ก.ย. 2569 · เทสต์ 69 + 18 จุดผ่านหมด_

ปิดช่องว่างข้อ **70, 68, 69, 71, 72** ในเอกสาร `claude/flowaccount-teardown.md` ครบทั้งรอบ

---

## แนวคิด: ของสี่อย่างนี้คือของชิ้นเดียวกัน

ในเอกสารถอดระบบแยกไว้เป็นสี่ฟีเจอร์ — รับชำระจริง · แบ่งงวด · ใบมัดจำ · ใบลดหนี้
แต่ในเชิงข้อมูลมันคือ **รายการเงินหนึ่งรายการที่ผูกกับเอกสารและงาน** ต่างกันแค่ป้ายกำกับ

จึงทำเป็นสมุดเล่มเดียว `DB.pays[]` มีสามประเภท

| ประเภท | คืออะไร | เงินเข้าบัญชีจริงไหม |
|---|---|---|
| `in` | รับชำระเข้าเอกสาร | เข้า |
| `deposit` | รับมัดจำล่วงหน้า ยังไม่ผูกใบไหน | เข้า |
| `apply` | ย้ายมัดจำมาตัดยอดเอกสาร | ไม่เข้า เป็นการย้ายเงินเก่า |

ใบลดหนี้/ใบเพิ่มหนี้ไม่เก็บซ้ำในสมุด แต่อ่านจากตัวเอกสารโดยตรง จะได้ไม่มีเลขสองที่ไม่ตรงกัน

**ผลที่ตามมา** รอบ 2 ค่าใช้จ่ายลงสมุดเล่มเดียวกันได้ด้วยเครื่องหมายลบ
แล้วหน้ากำไรจริงต่องานก็แค่บวกลบในสมุดนั้น ไม่ต้องเขียนใหม่

---

## สิ่งที่เปลี่ยนจริง

### 1. ค้างรับเปลี่ยนจาก "เดา" เป็น "จริง"

เดิมค้างรับ = ยอดเต็มของใบที่สถานะยังไม่ใช่ `paid` — เก็บมาแล้วครึ่งหนึ่งก็ยังขึ้นเต็มจำนวน
ตอนนี้ **ค้างรับ = ยอดตามเอกสาร − เงินที่รับจริง − มัดจำที่ตัดเข้า − ใบลดหนี้ + ใบเพิ่มหนี้**

แก้ให้ตรงกันทุกหน้า: ภาพรวม · รายชื่อลูกค้า · หน้าลูกค้ารายคน · รายการเอกสาร · กล่องต้องตามต่อ

### 2. ภาษีที่ลูกค้าหักไว้ ไม่ใช่หนี้ค้าง

ลูกค้าหัก 3% ไว้แล้วโอนที่เหลือมา — เงินส่วนนั้นไม่เข้าบัญชีเรา แต่ถือว่าชำระแล้ว
ระบบแยกสองช่องนี้ออกจากกัน **ยอดค้างนับรวม แต่เงินเข้าจริงไม่นับ** พร้อมช่องค่าธรรมเนียมด้วย

### 3. มัดจำที่รับก่อนออกใบได้

รับมัดจำจากหน้าลูกค้าได้เลยตั้งแต่ยังไม่มีใบแจ้งหนี้ ผูกกับงานไว้
พอออกใบแล้วกดปุ่ม "หักมัดจำ" ตัดเข้าใบนั้น ระบบกันไม่ให้หักเกินยอดที่ถือไว้

### 4. งวดชำระรู้ว่างวดไหนเก็บครบแล้ว

เดิมงวดเป็นแค่ข้อความไว้พิมพ์ท้ายใบ ตอนนี้มีวันครบกำหนดรายงวด
และไล่ตัดเงินที่รับจริงตามลำดับงวด ขึ้นป้ายให้เห็นว่า **ครบแล้ว · ขาดอีกเท่าไร · เลยกำหนด · รอเก็บ**

### 5. ใบลดหนี้ / ใบเพิ่มหนี้

เพิ่มชนิดเอกสาร `CN` และ `DN` ออกจากใบกำกับภาษีได้โดยตรง อ้างเลขที่และวันที่ใบเดิมให้อัตโนมัติ
ยอดในใบลดหนี้ไปลดยอดค้างของใบแม่ทันที

### 6. ยกเลิกเอกสารโดยเลขไม่ขาดช่วง

สรรพากรต้องการให้เลขที่เอกสารเรียงต่อเนื่อง จะลบทิ้งไม่ได้
ปุ่ม "ยกเลิกเอกสาร" เก็บเลขไว้ บันทึกวันที่และเหตุผล ขึ้นแถบแดงบนหัวเอกสาร
และตัดออกจากยอดขาย ภาษีขาย และยอดค้างรับทั้งหมด

### 7. อายุหนี้ (Aging)

แบ่ง 5 ช่วง — ยังไม่ถึงกำหนด · เลย 1–30 · 31–60 · 61–90 · เกิน 90 วัน
ขึ้นบนหน้าภาพรวมสองกล่อง และในกล่องต้องตามต่อบอกด้วยว่าใบไหนเก็บมาแล้วบางส่วนแต่ยังขาด

---

## เรื่องที่แก้ไปด้วยเพราะเจอระหว่างทาง

**เอกสารถูกนับซ้ำ** เดิมถ้าแปลงใบแจ้งหนี้เป็นใบกำกับภาษี ยอดค้างจะขึ้นสองใบ
ตอนนี้ใบที่ถูกต่อยอดไปแล้วจะไม่ถูกนับ และถ้าใบใหม่โดนยกเลิก ยอดจะเด้งกลับไปที่ใบเดิมให้เอง

---

## เทสต์

`node test/ledger.js` — เปิดเซิร์ฟเวอร์ทดสอบเองแล้วรันในเบราว์เซอร์จริง 18 จุด
ครอบคลุมรับบางส่วน · ภาษีหัก ณ ที่จ่าย · มัดจำ · ใบลดหนี้ · เอกสารยกเลิก · นับซ้ำ · งวดชำระ

`node test/smoke.js` — 69 จุดเดิม ยังผ่านครบ ไม่มีอะไรพังตาม

---

## แก้ข้อมูลผิดในเอกสารถอดระบบ

บรรทัด 133 เตือนว่าเพดาน **16 MB** จะเป็นปัญหาตอนเก็บรูปบิลในรอบ 2 — **ไม่จริงแล้ว**

16 MB คือเพดานของยุคที่หลังบ้านยังบันทึกทับตัวเอง ตอนนี้ระบบอยู่บน MongoDB
รูปถูกดึงออกไปเก็บใน collection แยกตั้งแต่ตอนบันทึก (`src/routes/adminstate.js`)
เพดานจริงคือ **512 MB ของ Atlas ฟรี** (`src/db.js`) และมีสคริปต์ย้ายไป R2 เขียนรออยู่แล้ว

**รอบ 2 เริ่มได้เลย ไม่ต้องรอย้าย storage**

---

## รอบต่อไป

**รอบ 2 — ฝั่งจ่ายและกำไรจริงต่องาน** คือจุดที่ FlowAccount ไม่มี
1. ผู้ขาย / ช่าง / ผู้รับเหมาช่วง
2. บันทึกค่าใช้จ่าย แนบรูปบิล ผูกกับงาน — ลงสมุดเงินเล่มเดิมด้วยเครื่องหมายลบ
3. หน้ากำไรจริงต่องาน = เงินเข้า − ค่าใช้จ่ายจริง เทียบกับต้นทุนที่ประเมินไว้ใน BOQ

**ก่อนใช้ออกเอกสารจริง** ยังต้องกรอกเลขผู้เสียภาษี ที่อยู่จดทะเบียน และบัญชีธนาคาร
ที่ตั้งค่า → ข้อมูลบริษัท ไม่งั้นใบกำกับพิมพ์ออกมาไม่ครบตามที่สรรพากรกำหนด
