# แก้รอบสอง — 30 ส.ค. 2569

เทสต์ **69 จุด ผ่านหมด** (เพิ่มจาก 68) และไล่ทดสอบในเบราว์เซอร์จริงทุกข้อ

---

## 2. กดเลือกต้นไม้แล้วไม่ลงในช่องรายการ · ไม่เห็นการเลือกขนาด — **แก้แล้ว**

ไล่ดูแล้วพบว่าปุ่มไม่ได้พัง แต่**ไม่มีอะไรให้เลือก**

หน้าต่าง "เลือกจากคลังของ" เดิมดึงจาก **คลังของ** อย่างเดียว ซึ่งเป็นคนละชุดกับ
**พันธุ์ไม้ 112 ชนิด** ในแคตตาล็อก ถ้าคลังของว่าง หน้าต่างก็ว่าง กดยังไงก็ไม่มีอะไรลง
และช่องเลือกขนาดจะโผล่เฉพาะรายการที่ตั้งตารางขนาดไว้แล้วเท่านั้น จึงไม่เห็นเช่นกัน

**เปลี่ยนเป็น** หน้าต่างมีสองแท็บ

| แท็บ | ได้อะไร |
|---|---|
| **คลังของ** | รายการที่มีราคาทุนแล้ว เลือกแล้วได้ราคาทันที |
| **พันธุ์ไม้** | ทั้ง 112 ชนิด ชนิดไหนยังไม่มีในคลัง **ระบบสร้างรายการให้เลย** |

เลือกจากพันธุ์ไม้แล้วระบบจะ
- สร้างรายการในคลังของให้ ผูกรหัสพันธุ์ไม้ให้เรียบร้อย
- **ตั้งโหมดขนาดให้ตามหมวดอัตโนมัติ** — ไม้ระดับสูงได้โหมดไม้ล้อม (สูง + หน้า) ·
  ไม้ริมรั้วได้โหมดความสูง · ไม้ระดับต่ำได้โหมดถุง/กระถาง 4 6 8 11 นิ้ว
- ใส่ราคาทุนเป็น 0 พร้อมหมายเหตุ **"ยังไม่ได้ใส่ราคาทุน"** ติดไว้ที่บรรทัด
  และขึ้นป้ายแดงในตารางคลัง เพื่อไม่ให้เผลอออกใบเสนอราคาที่ราคาเป็นศูนย์

ถ้าคลังของว่างเปล่า หน้าต่างจะขึ้นข้อความบอกพร้อมปุ่มสองปุ่ม
"เลือกจากพันธุ์ไม้ 112 ชนิด" และ "ใส่รายการตัวอย่าง 20 รายการ" แทนที่จะว่างเฉย ๆ

---

## 4. ปัดราคาขึ้นให้ลงท้ายด้วย 0 — **แก้แล้ว**

**ราคาต่อหน่วยที่เสนอลูกค้าถูกปัดขึ้นให้ลงท้ายด้วย 0 เสมอ** เช่น ทุน 400 บวก 33% = 532 → **540**
ปัดขึ้นอย่างเดียว ไม่ปัดลง กำไรจึงไม่หายจากการปัดเศษ · **ทุนไม่ถูกปัด** ปัดเฉพาะราคาขาย

ใช้ทุกที่ที่คิดราคาให้ลูกค้า — ตารางคลังของ · ตารางขนาด · หน้าต่างแก้ไขรายการ ·
และตอนกดเพิ่มของเข้าใบเสนอราคา ปัดที่ยอดต่อหน่วยก่อน แล้วค่อยแบ่งกลับเป็น
ค่าวัสดุกับค่าแรงตามสัดส่วนทุน ยอดที่ลูกค้าเห็นจึงลงท้าย 0 จริง ไม่ใช่แค่ปัดทีละช่อง

> **สูตรคิดเงินเดิมไม่ถูกแตะ** ยังเป็น `(ค่าของ + ค่าแรง + ขนส่ง + เครื่องจักร) × (1 + %กำไร/100)`
> เพิ่มแค่การปัดขั้นสุดท้าย ถ้าอยากเปลี่ยนเป็นปัดหลักร้อย แก้ค่า `ROUND_UP_TO` ค่าเดียว

---

## 5. ปุ่มแก้ไขในรายการ — **เพิ่มแล้ว**

ทุกบรรทัดในใบเสนอราคามีปุ่ม **"แก้"** เปิดหน้าต่างที่รวมทุกช่องของบรรทัดนั้นไว้ที่เดียว
ชื่อรายการ · หมายเหตุ · **ขนาด** · ปริมาณ · หน่วย · ทุนวัสดุ · ทุนค่าแรง · % กำไร
พร้อมกล่องเขียวโชว์ราคาต่อหน่วยและยอดรวมแบบสด ๆ

**เปลี่ยนขนาดได้ที่นี่ที่เดียว** เลือกขนาดใหม่แล้วระบบดึงทุนใหม่จากคลังของให้อัตโนมัติ
และแก้ชื่อรายการให้ตรงขนาดด้วย เช่น "ไทรเกาหลี สูง 1.5 ม." → "ไทรเกาหลี สูง 2.5 ม."

ตารางรายการกว้างมาก บนจอเล็กเลื่อนหาช่องยาก ปุ่มนี้จึงช่วยได้จริงตอนใช้งานบนมือถือ

---

## 3. หน้าเว็บหลักภาษาอังกฤษและจีน — **เสร็จแล้ว**

มี **ปุ่มสลับภาษา ไทย / EN / 中文** อยู่บนแถบเมนู (จอใหญ่) และในเมนูสไลด์ (มือถือ)
เลือกแล้วจำไว้ ครั้งหน้าเข้ามาได้ภาษาเดิม

แปลครบ **161 จุด** ทั้งเมนู เนื้อหา คำถามที่พบบ่อย และ **ขั้นตอนการทำงานทั้ง 6 ขั้น**
ที่สร้างด้วยจาวาสคริปต์ รวมถึงข้อความในแท็ก `alt` และ `meta description` สำหรับ SEO
ทดสอบแล้ว **ไม่มีภาษาไทยตกค้างแม้แต่จุดเดียว** ทั้งภาษาอังกฤษและจีน

วิธีทำ: ไม่ได้แก้โครงหน้าเว็บเลย แต่ใช้พจนานุกรมใน `public/js/i18n.js`
ที่คีย์ด้วย "ข้อความไทยต้นฉบับ" แล้วเดินไล่เปลี่ยนตอนกดสลับภาษา
ข้อความไหนไม่มีคำแปล จะคงภาษาไทยไว้ ไม่หายไป **แก้คำแปลได้ที่ไฟล์เดียว**

ภาษาจีนต่อฟอนต์ระบบให้แล้ว (PingFang SC / Microsoft YaHei) ไม่ได้โหลดฟอนต์จีนจากเน็ต
เพราะฟอนต์จีนไฟล์ใหญ่หลายเมกะไบต์ จะทำให้หน้าเว็บช้าลงมาก

**ข้อจำกัดที่ควรรู้:** การสลับภาษาเกิดในเบราว์เซอร์ Google จึงยังเก็บ index เฉพาะภาษาไทย
ถ้าวันหนึ่งอยากให้ลูกค้าจีนหาเจอใน Google จริง ๆ ต้องทำเป็นคนละ URL
(`/en/` `/zh/`) ซึ่งทำเพิ่มได้ทีหลังโดยใช้พจนานุกรมชุดเดิม

**ยังไม่ได้แปล:** หน้าเครื่องมือจัดสวน (`/tool/`) และหน้าแคตตาล็อก (`/catalog/`)
ยังเป็นภาษาไทยล้วน — ถ้าต้องการให้บอก

---

## เจอบั๊กเพิ่มระหว่างทาง — **รูปบนหน้าเว็บ 404 ทั้งหมด**

ไม่ได้อยู่ในรายการที่แจ้ง แต่เจอตอนถ่ายภาพหน้าจอมาตรวจ

หน้าเว็บอ้างรูปด้วย `img/garden-front-after.webp` ซึ่งกลายเป็น `/img/garden-front-after.webp`
แต่เส้นทาง `/img/:id` ที่ใช้เสิร์ฟรูปจากฐานข้อมูลดักไว้ก่อน แล้วตอบ 404
เพราะชื่อไฟล์ไม่ใช่รหัสในฐานข้อมูล **รูปหน้าแรกกับรูปผลงานจึงไม่ขึ้นเลยบนเว็บจริง**

แก้โดยให้เส้นทางนั้นรับเฉพาะรหัสฐานข้อมูล (เลขฐานสิบหก 24 ตัว) นอกนั้นปล่อยผ่าน
ไปให้ตัวเสิร์ฟไฟล์นิ่งจัดการ · เพิ่มเทสต์คุมไว้แล้ว ไม่ให้พลาดซ้ำ

**และแก้อีกจุด** ปุ่ม "ล็อกหน้าจอ" ที่เพิ่มไปรอบก่อน ลอยทับปุ่ม "บันทึก" มุมขวาล่าง
ตอนเปิดหน้าต่างซ้อน ทำให้กดบันทึกไม่ได้บนจอเล็ก ตอนนี้ซ่อนอัตโนมัติเมื่อมีหน้าต่างเปิดอยู่

---

## 1. เว็บตื่นหรือยัง — **ตรวจจากที่นี่ไม่ได้**

ทั้งเครื่องที่รันงานนี้และเครื่องเบลล์ถูกปิดกั้นไม่ให้ต่อออกอินเทอร์เน็ตไปที่ `onrender.com`
จึงยืนยันให้ไม่ได้ **ต้องเปิดดูเองในเบราว์เซอร์**

เปิด `https://alwaystrees.onrender.com/api/health`

| เห็นอะไร | แปลว่า |
|---|---|
| `{"ok":true,"db":"up"}` | เว็บตื่นและต่อฐานข้อมูลได้ **ปกติดี** |
| `{"ok":true,"db":"down"}` | เว็บตื่นแต่ต่อ Atlas ไม่ได้ → เช็ก `MONGODB_URI` และ Network Access `0.0.0.0/0` |
| ช้าประมาณหนึ่งนาทีแล้วขึ้น | ปกติของแพลนฟรี ไม่ใช่ error |
| หน้าเปล่า / ต่อไม่ติด | ยังไม่ได้สร้าง Web Service หรือ deploy ล้มเหลว → ดู Logs ที่ Render |

ถ้ายังไม่ได้ตั้ง **cron-job.org** ให้ตั้งด้วย (README ข้อ 6.2)
เพราะตัวปลุกของ GitHub จะถูกปิดเองถ้า repo เงียบ 60 วัน

---

## ขั้นต่อไป

```bash
cd ~/Desktop/Pooh/always-trees-admin-handover/app
node test/smoke.js          # ต้องได้ ผ่าน 69 · ตก 0
git add -A
git commit -m "เลือกพันธุ์ไม้เข้าใบเสนอราคา + ปุ่มแก้รายการ + ปัดราคา + 3 ภาษา + แก้รูป 404"
git push
```
