# แก้ตามที่แจ้ง — 30 ส.ค. 2569

รันเทสต์แล้ว **68 จุด ผ่านหมด** (`node test/smoke.js`) และไล่ทดสอบในเบราว์เซอร์จริงอีกรอบ

---

## 8 ข้อที่แจ้งว่าเป็นปัญหา

### 1. รีโหลดหน้าหลังบ้านแล้วยังเข้าได้อยู่ ต้องถามรหัสใหม่ — **แก้แล้ว**
เดิมคุกกี้อายุ 14 วัน เครื่องเดิมเปิดค้างไว้เข้าได้ตลอด ตอนนี้เป็นสี่ชั้น

| ชั้น | ผล |
|---|---|
| คุกกี้แบบ session | **ปิดเบราว์เซอร์แล้วหลุดทันที** |
| ไม่แตะเกิน 30 นาที | ถามรหัสใหม่ |
| ใช้งานอยู่ | ต่ออายุให้เอง ไม่โดนเตะกลางคัน |
| ครบ 12 ชม. นับจากล็อกอิน | ถามรหัสใหม่ ต่อให้ใช้งานตลอด |

เพิ่ม **ปุ่ม “ล็อกหน้าจอ”** มุมขวาล่าง กดตอนลุกจากโต๊ะ ออกทันที
และมีแถบเตือนกลางจอเมื่อเหลือไม่ถึง 2 นาที จะได้กดบันทึกก่อน
ปรับความเข้มได้ที่ Render → Environment → `ADMIN_IDLE_MINUTES`

> **ไม่ได้ทำให้ถามรหัสทุกครั้งที่รีโหลด** เพราะระหว่างทำใบเสนอราคายาว ๆ
> การรีเฟรชเป็นเรื่องปกติ ถ้าถามทุกครั้งจะใช้งานจริงไม่ไหว
> ถ้าอยากได้แบบนั้นจริง ๆ ตั้ง `ADMIN_IDLE_MINUTES=5` เอาได้

### 2–4 + สิ่งที่อยากเพิ่มข้อ 1 · ราคาตามขนาด — **แก้แล้ว**
คลังของเก็บ **ตารางขนาดในรายการเดียว** ไม่ต้องสร้างรายการซ้ำทีละขนาด

| โหมด | ใช้กับ | แยกราคาตาม |
|---|---|---|
| ไม้เล็ก | ต้นไม้เล็ก | ขนาดถุง/กระถาง เป็นนิ้ว ตั้งค่าเริ่มให้ **4 · 6 · 8 · 11** |
| ไม้ริมรั้ว | ไม้ริมรั้ว | ความสูง (ม.) |
| ไม้ล้อม | ไม้เดี่ยว ไม้ล้อม | ความสูง (ม.) **+ หน้า (นิ้ว)** |

ตอนทำใบเสนอราคา กด “เลือกจากคลังของ” จะมี **ช่องเลือกขนาด** ในแต่ละแถว
เลือกแล้วทุนและชื่อรายการเปลี่ยนตามขนาดให้เอง เช่น “ไทรเกาหลี ถุง 11 นิ้ว”
ช่องทุนของขนาดไหนที่เว้นว่าง จะใช้ค่าจาก “ต้นทุนตั้งต้น” ให้อัตโนมัติ

**เรื่อง “หน้า” — เอกสารเดิมเขียนผิด แก้ให้แล้ว**
`spec/สคีมาและสัญญาเชื่อมข้อมูล.md` เคยระบุว่า `caliper_in` คือ *เส้นผ่านศูนย์กลาง*
แต่ที่ยืนยันมาคือ **วัดด้วยสายวัดพันรอบต้น = เส้นรอบวง** ซึ่งต่างกันประมาณ 3 เท่า
จึงเลิกใช้ `caliper_in` เปลี่ยนเป็น `girth_in` และแก้เอกสารสเปกให้ตรงแล้ว
ค่าที่เคยกรอกไว้จะขึ้น ⚠ ในตารางคลัง **ห้ามเอาไปคิดราคา ต้องวัดใหม่**

### 5. ขึ้นว่า “บันทึกถาวรยังไม่พร้อม” — **แก้แล้ว**
เป็นโค้ดที่ค้างมาจากสมัยที่หลังบ้านยังเป็น artifact — มันไปถาม `window.claude`
ซึ่งไม่มีบนเซิร์ฟเวอร์จริง จึงตอบว่าไม่พร้อมทุกครั้ง **ทั้งที่บันทึกได้ปกติ**
ตอนนี้ถามเซิร์ฟเวอร์จริงแทน (`HEAD /api/admin/state`)
ปกติจะขึ้นว่า **“บันทึกอัตโนมัติขึ้นเซิร์ฟเวอร์”** ถ้าขึ้นแดงแปลว่าต่อเซิร์ฟเวอร์ไม่ได้จริง

### 6. เว็บไม่ตื่น ปลุกไม่สำเร็จ — **แก้แล้ว + ให้ตั้ง cron-job.org**
เจอสองสาเหตุ

1. `/api/health` เดิมตอบ **503 เมื่อฐานข้อมูลสะดุด** → ทั้ง health check ของ Render
   และตัวปลุกถือว่าล้มเหลว ทั้งที่เว็บตื่นอยู่ ตอนนี้ตอบ `200` เสมอถ้าเว็บตื่น
   (สถานะฐานข้อมูลอยู่ในเนื้อคำตอบ) และเพิ่ม `/api/health/db` ไว้ดูสถานะจริง
2. workflow เดิมพังทันทีถ้ายังไม่ได้ตั้ง secret `APP_URL` — ตอนนี้มีค่าสำรองให้แล้ว

**และควรตั้ง cron-job.org ควบคู่** เพราะตารางของ GitHub Actions ไม่การันตีเวลา
และ **ถูกปิดเองถ้า repo เงียบ 60 วัน** — เป็นสาเหตุที่พบบ่อยที่สุดของ “ปลุกไม่ตื่น”
วิธีตั้งทีละขั้นอยู่ใน `README.md` หัวข้อ **6.2** พร้อมตารางไล่หาสาเหตุ

### 7 + 8. กดหมวดแล้วขึ้นทั้งหมด · รหัสกับประเภทไม่ตรงกัน — **แก้แล้ว รากเดียวกัน**
ตอนแรกตรวจไฟล์ `data/plants.json` แล้วไม่พบปัญหา (รหัสตรงหมวดครบ 112)
สาเหตุจริงอยู่ที่ **ขั้นตอนคัดลอกข้อมูลจากหลังบ้านไปแคตตาล็อก**

หลังบ้านเก็บหมวดเป็น **ชื่อไทย** (`"ไม้ระดับสูง"`) แต่แคตตาล็อกกรองด้วย **รหัสสั้น** (`"sung"`)
เซิร์ฟเวอร์คัดลอกดิบ ๆ ไป → พอกดบันทึกในหลังบ้านครั้งแรก ข้อมูลบนเว็บก็เพี้ยนทันที
ป้ายหมวดเลยไม่ตรงกับตัวนำหน้ารหัส TL/HG/SH/GC/ST และตัวกรองหมวดใช้ไม่ได้

แก้สามจุด
- เซิร์ฟเวอร์แปลงหมวดเป็นรหัสสั้นก่อนเสมอ · `alt` กลับเป็นข้อความ · ใช้ id เดิมของแคตตาล็อก
- **บังคับให้รหัสขึ้นต้นตรงกับหมวดเสมอ** ถ้าไม่ตรงจะแก้ให้อัตโนมัติ
- อัปเดตชุดตั้งต้น 112 รายการในหลังบ้านให้ตรงกับ `data/plants.json`
  (ของเดิม `w_m`/`r_m` เป็น 0 หมด กดบันทึกทีเดียวจะทับค่าดี ๆ 61 รายการทิ้ง)

ทดสอบในเบราว์เซอร์จริงหลังแก้ — กดครบทุกหมวดได้ตามนี้

| หมวด | จำนวนการ์ด | รหัสที่ขึ้น |
|---|---|---|
| ไม้ระดับสูง | 31 | TL เท่านั้น |
| ไม้ริมรั้ว | 20 | HG เท่านั้น |
| ไม้ระดับต่ำ | 30 | SH เท่านั้น |
| ไม้คลุมดิน | 17 | GC เท่านั้น |
| หิน | 14 | ST เท่านั้น |

---

## สิ่งที่อยากเพิ่มข้อ 2 · ภาษาอังกฤษและจีน — **ยังไม่ได้ทำ**

ไม่ใช่งานโค้ด แต่เป็นงานแปลข้อความประมาณ 200 จุดให้ได้โทนแบรนด์
“ความงามที่จริงใจ ไม่ขายฝัน” ในสามภาษา ทำครึ่ง ๆ กลาง ๆ แล้วหน้าเว็บปนภาษา
จะแย่กว่าไม่ทำ จึงขอแยกเป็นงานถัดไปหลังจากเว็บขึ้น Render เรียบร้อยแล้ว

---

## ต้องทำอะไรต่อ

1. `git add -A && git commit -m "แก้บั๊ก 8 ข้อ + ราคาตามขนาด" && git push`
2. Render จะ deploy ให้เอง รอสักครู่
3. ตั้ง cron-job.org ตาม README ข้อ 6.2
4. เข้า `/admin/` → คลังของ → ใส่ราคาทุนจริงพร้อมตารางขนาด
   **ห้ามออกใบเสนอราคาจริงก่อนแก้ราคาให้ตรงราคาซื้อจริง**
